# Optional: absolute path to ffmpeg.exe or a folder containing ffmpeg.
# Leave unset when ffmpeg is already available on PATH.
FFMPEG_PATH=""

# Optional: public URL for deployed environments.
APP_URL="http://127.0.0.1:3000"
